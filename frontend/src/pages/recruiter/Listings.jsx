import { Row, Col } from "reactstrap";

import PageContainer from "components/PageContainer";

const Listings = () => {
    return (
        <PageContainer navbar>
            <h1> recruiter listings </h1>
        </PageContainer>
    );
};

export default Listings;
