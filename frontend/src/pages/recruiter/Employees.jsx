import PageContainer from "components/PageContainer";

const Employees = () => {
    return (
        <PageContainer>
            <h1> recruiter employees </h1>
        </PageContainer>
    );
};

export default Employees;
